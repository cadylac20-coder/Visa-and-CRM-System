services:
  - type: web
    name: mkov-visa-system
    env: python
    buildCommand: "apt-get install -y tesseract-ocr && pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: SECRET_KEY
        generateValue: true
